// Deprecated: kept for existing consumers. Use `io.github.siloverse.jvm-application`, which
// supports Java, Kotlin and mixed sources. This id only forces Kotlin on unconditionally.
plugins {
    id("io.github.siloverse.kotlin-library")
    application
}
